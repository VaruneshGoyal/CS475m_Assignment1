To compile type "make."

This is a simple 2D drawing program that can draw lines, filled triangles etc. (like a basic version of paint).
Bresenham's algorithm for line drawing, floodfill etc are implemented.
You can change pen color, size, fill color, save your work (not in image format but a simple txt file), load back from the txt file, undo etc.
